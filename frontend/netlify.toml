[build]
  publish = "dist"
  command = "npm run build"
  base = "frontend"

[build.environment]
  VITE_API_URL = "https://schemescan-qrha.onrender.com/api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
